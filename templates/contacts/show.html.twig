{% extends 'base.html.twig' %}

{% block title %}Contacts{% endblock %}

{% block body %}
    <h1>Contacts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact.id }}</td>
            </tr>
            <tr>
                <th>NomContact</th>
                <td>{{ contact.nomContact }}</td>
            </tr>
            <tr>
                <th>PrenomContact</th>
                <td>{{ contact.prenomContact }}</td>
            </tr>
            <tr>
                <th>EntrepriseContact</th>
                <td>{{ contact.entrepriseContact }}</td>
            </tr>
            <tr>
                <th>MessageContact</th>
                <td>{{ contact.messageContact }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contacts_index') }}">back to list</a>

    <a href="{{ path('contacts_edit', {'id': contact.id}) }}">edit</a>

    {{ include('contacts/_delete_form.html.twig') }}
{% endblock %}
